<div id="fullscreen_bg" class="fullscreen_bg"/>

<div class="container">

	<h1 class="form-signin-heading text-muted center-text">Sign up</h1>

	<%= render 'shared/flash_messages' if !flash.empty? %>

	<%= form_for @business do |b| %>
	  	<%= b.text_field :name, class: "form-control", placeholder: "Name of your business"%>
	  	<%= b.text_field :email, class: "form-control", placeholder: "Email address"%>
	  	<%= b.password_field :password, class: "form-control", placeholder: "Password"%><br/>
	  	<%= b.submit "Create Account", class:"btn btn-lg btn-primary btn-block"%>
	<% end %>
</div>

